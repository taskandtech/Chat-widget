<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Moonlit Chat Widget — Demo</title>
  <link rel="stylesheet" href="style.css">
  <style>
    /* Demo-only backdrop so the widget is visible outside OBS/StreamElements.
       This does not affect the actual widget styling in style.css. */
    body {
      background: radial-gradient(circle at 50% 30%, #2b1f33 0%, #100b16 70%) !important;
      display: flex;
      align-items: flex-end;
      justify-content: center;
      min-height: 100vh;
    }
  </style>
</head>
<body>

  <div id="chat-widget">
    <div class="chat-header" aria-label="Chat">
      <span class="header-line"></span>
      <span class="header-title">CHAT</span>
      <span class="header-line"></span>
    </div>

    <div id="test-badge">TEST MODE</div>
    <div id="log" class="chat-log" aria-live="polite"></div>
  </div>

  <script src="script.js"></script>
</body>
</html>
